services:
  db:
    image: postgres:16-alpine
    container_name: pfk-workspace-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-pfk_workspace}
      POSTGRES_USER: ${POSTGRES_USER:-pfk}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pfk_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pfk_workspace_db:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - pfk-workspace

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: pfk-workspace-pgadmin
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@pfk.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin}
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    networks:
      - pfk-workspace

networks:
  pfk-workspace:
    driver: bridge

volumes:
  pfk_workspace_db:
